# Frontend environment variables (create client/.env for local dev if needed)

# Live backend URL (Render). Set this before building for production.
# Example: https://ddjc-backend.onrender.com
VITE_API_URL=http://localhost:5000
